// Only this email is allowed to use the app. Set it to your own email —
// anyone who logs in or signs up with a different address is signed out
// immediately, so a stranger finding your site can never get in.
export const OWNER_EMAIL = 'YOUR_EMAIL@example.com';
